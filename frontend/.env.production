# CloudFront distribution URL — populate after running: cdk deploy FrontendStack
# Copy the CloudFrontUrl output value and paste it here, then rebuild and sync to S3:
#   npm run build
#   aws s3 sync dist/ s3://ski-app-frontend-{account}-{region}/ --delete
#   aws cloudfront create-invalidation --distribution-id {id} --paths "/*"
VITE_API_BASE_URL=https://REPLACE_WITH_CLOUDFRONT_DOMAIN
